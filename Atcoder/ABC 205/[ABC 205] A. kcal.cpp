#include<bits/stdc++.h>
using namespace std;
int main(void) {
	ios::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
	double a, b; cin >> a >> b;
	cout << fixed;
	cout.precision(10);
	cout << a * (b / 100);
}